# API

The Block Protocol API is used exclusively to interface with the Þ Hub.

This allows for users to programmatically both **query for** and **use** blocks, types and services, as well as securely **authenticate** themselves with external services.
