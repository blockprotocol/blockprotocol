{
  "$id": "https://blockprotocol.org/types/modules/graph/property-type-reference",
  "title": "Property Type Reference",
  "description": "A reference to a property type.",
  "type": "object",
  "properties": {
    "$ref": {
      "$comment": "The versioned URL of a property type (the $id of the property type's schema)",
      "$ref": "https://blockprotocol.org/types/modules/graph/versioned-url"
    }
  },
  "required": [
    "$ref"
  ],
  "additionalProperties": false
}
